import { StopClick } from './stop-click';

describe('StopClick', () => {
  it('should create an instance', () => {
    const directive = new StopClick();
    expect(directive).toBeTruthy();
  });
});
