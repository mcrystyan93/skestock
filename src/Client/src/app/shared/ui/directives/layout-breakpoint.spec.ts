import { LayoutBreakpoint } from './layout-breakpoint';

describe('LayoutBreakpoint', () => {
  it('should create an instance', () => {
    const directive = new LayoutBreakpoint();
    expect(directive).toBeTruthy();
  });
});
